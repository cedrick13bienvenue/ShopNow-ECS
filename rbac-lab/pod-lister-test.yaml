apiVersion: v1
kind: Pod
metadata:
  name: pod-lister-test
  namespace: default
spec:
  serviceAccountName: pod-lister-sa
  containers:
    - name: kubectl
      image: bitnami/kubectl:latest
      command: ["sleep", "3600"]
